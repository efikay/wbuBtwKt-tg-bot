package com.efikay.wbubtw.random.service

class RandomServiceConfig {
    companion object {
        // TODO: Add variable range of random (on random service construction)
        const val MIN_RANDOM_NUMBER = 1
        const val MAX_RANDOM_NUMBER = 100
    }
}